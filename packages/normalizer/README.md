# Digithon Normalizer

Runs the Python normalization IP over messy retail CSV rows.

```bash
poetry run normalizer --input db/seed/messy_sales.csv
```

Output is JSON shaped for the worker and API contracts.
